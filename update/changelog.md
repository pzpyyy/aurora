11.27晚上更新日志。
❄优化规则清理的架构优化：异常安全路径遍历系统。当前路径子树跳过，继续其他分支遍历，最大化可访问路径的清理覆盖率
❄优化空文件清理的开销。哈希集合去重:使用 unordered_set 自动去重，避免std::find的O(n)查找,白名单检查缓存降低相同路径的重复检查以及在遍历时计算目录深度，避免重复的字符串长度计算，减少容器操作，消除多次排序和去重操作，使用 reserve 预分配内存。从原先的几秒清理完成到现在更快清理完成更快结束清理，占用cpu资源更低。消除重复的路径检查
❄优化系统和应用缓存清理：第一：系统调用减少: 消除重复的 popen 和文件打开操作。第二：内存访问优化: 使用哈希集合替代线性搜索。第三：减少多余的i/o操作
❄优化qq和微信深度清理逻辑：第一：修改计数逻辑实现精确计数。第二：优化性能开销，清理时间几秒甚至1秒完成，大大降低清理时的开销。第三：单次目录遍历：避免重复扫描来降低开销，优化性能。
❄优化公共头文件utils。
🌸第一：所有日志系统优化：从每次调用2次系统调用 → 每秒1次系统调用。以及减少磁盘压力，相比于大多数日志函数，每次都刷盘，本次更新采用条件刷盘，极大降低磁盘压力。
🌸第二：文件操作优化 ，module.prop更新数据优化。减少io的调用


❄web界面支持更换背景图，在更换背景图的时候存储在web缓存，避免了每次进入web加载大概0.5s才出现背景，实现进web秒更新背景图，不会出现白色背景和自定义背景的转化延迟。
❄切换背景教程：1.把你的背景图命名为background.jpg替换/data/adb/modules/Aurora/admin/background.jpg，然后进入web界面，点击我的页面，点击清除背景存储缓存即可更新背景图。
❄注意白色背景也要自己找个白色背景图放进入，避免后续更新选择继承以往配置会被默认背景覆盖，但是你有这个背景图文件的话无需担心，会继承你的背景图。


11.27上午更新日志
修复规则清理导致部分游戏资源因为规则增加导致资源被清理