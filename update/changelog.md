🌌1220：修复重定向挂载问题（应该吧嘻嘻，主要是管理器的service的sleep没用我测试才发现），修复通知显示定时未运行【实际是运行了的】


❄1219v1大版本更新❄可以对比下此版本和之前的版本清理所需时间以及去scene看清理进程所占cpu开销🌌此版本的系统和应用缓存清理应该不需要重启了。


🌌再讲解下定时规则。数字表示每多少分钟执行一次，数字:数字代表每天几点几分执行，数字 数字:数字代表每周几的几点几分执行，前一个数字是1-7代表周一到周七


🌌此版本进入深度优化v1版本。可能部分机型清理数目是0那就是不兼容此版本的优化逻辑。最大程度降开销，降低清理所需时间以及清理cpu资源的占用

🌌修复bug：修复规则清理导致的文件资源管理器找不到文件需要重启才恢复，以及和规则清理后scene不能切换温控需要重启才能切换温控策略的bug


🌌0.（系统和应用缓存清理）
可能不再需要重启了
使用更高效的方式替代filesystem::directory_iterator提升扫描速度
批量收集路径再删除，减少系统调用次数
直接使用高效的系统调用删除
合并用户目录遍历，避免重复扫描
保持原有清理逻辑不变


🌌1.（微信清理优化）
使用更高效的方式替代filesystem::directory_iterator提升扫描速度
批量收集路径再删除，减少系统调用次数
直接使用高效的系统调用删除
合并用户目录遍历，避免重复扫描
保持原有清理逻辑不变


🌌2.（QQ/TIM清理优化）
使用更高效的方式替代C++文件系统API
按目录分组规则，每个目录只扫描一次
批量删除收集的路径
直接系统调用删除，减少中间层开销
并行处理不同用户目录


🌌3.（空文件清理优化）
预编译白名单规则，提升匹配速度
使用底层方案遍历目录
内存映射文件读取白名单和扫描路径
批量快速删除空文件和空目录
快速父目录检查算法


🌌4.（规则垃圾清理优化）
采用高效通配符匹配算法
简化白名单检查逻辑
在路径展开阶段就排除白名单
使用高效逻辑直接删除文件
目录从深到浅排序删除


🌌5.（文件归类优化）
完全移除白名单逻辑
按扫描路径分组规则，每个目录只扫描一次
批量处理文件名冲突（使用哈希表）


🌌6. 扫描清理优化）
统一白名单逻辑（采用trash_cleaner的检查方式）
前置白名单过滤，在扫描阶段就排除
按扫描路径分组规则，避免重复扫描


🌌7. 定时开销优化：
减少不必要的操作，
移除唤醒锁，本身就不会唤醒，故移除，
优化堆栈的逻辑，减少每次执行完次任务加载下一个任务的逻辑